name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  cli:
    name: cli — typecheck + smoke test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cli
    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Sync template/ from repo root into cli/
        run: bun run sync-template

      - name: Smoke test — minimal profile
        run: |
          rm -rf /tmp/smoke-min
          bun run src/index.ts init /tmp/smoke-min --profile=minimal --ai=generic --name="CI Smoke (minimal)"
          test -f /tmp/smoke-min/docs/WORKFLOW.md
          test -f /tmp/smoke-min/docs/01-artefacts/arc42/09-decisions/README.md
          test -f /tmp/smoke-min/docs/01-artefacts/templates/5_adr.md
          test ! -d /tmp/smoke-min/docs/01-artefacts/user-manual
          test ! -d /tmp/smoke-min/.claude
          test -f /tmp/smoke-min/.pentaglyph

      - name: Smoke test — standard profile + claude hook
        run: |
          rm -rf /tmp/smoke-std
          bun run src/index.ts init /tmp/smoke-std --profile=standard --ai=claude --name="CI Smoke (standard)"
          test -f /tmp/smoke-std/docs/01-artefacts/diagrams/c4/workspace.dsl
          test -f /tmp/smoke-std/docs/01-artefacts/detailed-design/README.md
          test -f /tmp/smoke-std/.claude/rules/documentation.md
          ! grep -q '<placeholder>' /tmp/smoke-std/docs/WORKFLOW.md
          test -f /tmp/smoke-std/.pentaglyph
          grep -q '"version": "0.2.0"' /tmp/smoke-std/.pentaglyph

      - name: Smoke test — full profile
        run: |
          rm -rf /tmp/smoke-full
          bun run src/index.ts init /tmp/smoke-full --profile=full --ai=cursor --name="CI Smoke (full)"
          test -d /tmp/smoke-full/docs/01-artefacts/user-manual/tutorials
          test -d /tmp/smoke-full/docs/01-artefacts/user-manual/how-to
          test -d /tmp/smoke-full/docs/01-artefacts/user-manual/reference
          test -d /tmp/smoke-full/docs/01-artefacts/user-manual/explanation
          test -d /tmp/smoke-full/docs/01-artefacts/cost-estimates
          test -f /tmp/smoke-full/.cursor/rules/docs.md

      - name: Smoke test — add command
        run: |
          rm -rf /tmp/smoke-add
          bun run src/index.ts init /tmp/smoke-add --profile=minimal --ai=generic
          bun run src/index.ts add user-manual /tmp/smoke-add
          test -f /tmp/smoke-add/docs/01-artefacts/user-manual/README.md || \
            test -f /tmp/smoke-add/docs/user-manual/README.md
          # Re-running add should skip existing files
          OUT=$(bun run src/index.ts add user-manual /tmp/smoke-add)
          echo "$OUT" | grep -q '0 files written'

      - name: Smoke test — --include override
        run: |
          rm -rf /tmp/smoke-inc
          bun run src/index.ts init /tmp/smoke-inc --include=arc42,templates --ai=generic
          test -d /tmp/smoke-inc/docs/01-artefacts/arc42
          test -d /tmp/smoke-inc/docs/01-artefacts/templates
          test ! -d /tmp/smoke-inc/docs/01-artefacts/detailed-design
          test ! -d /tmp/smoke-inc/docs/01-artefacts/user-manual

      - name: Smoke test — --dry-run produces no files
        run: |
          rm -rf /tmp/smoke-dry
          bun run src/index.ts init /tmp/smoke-dry --profile=full --ai=claude --dry-run
          test ! -d /tmp/smoke-dry

      - name: Smoke test — AI_AGENT_HINT line in post-init output
        run: |
          rm -rf /tmp/smoke-hint
          OUT=$(bun run src/index.ts init /tmp/smoke-hint --profile=minimal --ai=generic)
          echo "$OUT" | grep -q '^AI_AGENT_HINT: kit=pentaglyph'

      - name: npm pack — verify tarball includes template/
        run: |
          npm pack --dry-run 2>&1 | tee /tmp/pack.log
          grep -q 'template/docs/WORKFLOW.md' /tmp/pack.log
          grep -q 'template/docs/01-artefacts/arc42/09-decisions/README.md' /tmp/pack.log
          grep -q 'template/docs/01-artefacts/templates/5_adr.md' /tmp/pack.log
